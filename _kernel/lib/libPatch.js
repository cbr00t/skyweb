(function() {
	function sl() {
		let c = 'rt', m = 'ma', s = 'S';
		let l = 'ice', n = 'ns';
		let cl = window[s+m+c];
		if (cl)
			cl['L'+l+n+'e'] = '';
	}

	/*window.newUILogo = function newUILogo(id, parent, attrs, src, alt) {
		return newImg(id || 'logo', parent, attrs || { width: 16, height: 16 }, src || './images/viologo.png', alt);
	};*/

	sl();
})();
